{
    "name": "user-crud",
    "scripts": {
        "test": "jest --config jest.config.js",
        "lint": "eslint .",
        "lint:fix": "eslint --fix .",
        "prettier": "prettier --check \"./**/*\"",
        "prettier:fix": "prettier --write \"./**/*\""
    },
    "devDependencies": {
        "aws-sdk": "^2.652.0",
        "aws-sdk-mock": "^5.1.0",
        "eslint": "^6.8.0",
        "prettier": "^2.0.2"
    }
}
